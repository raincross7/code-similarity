#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#include <iostream>
#include <ctype.h>
#include <string>
typedef long long ll;
using namespace std;

int main(){
   int a,b,c;
  cin>>a>>b>>c;
  if(a-b>c)
    cout<<"0"<<endl;
  else
    cout<<c-a+b<<endl;
    
    
  
    
}