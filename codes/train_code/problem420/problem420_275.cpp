#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    string a, b;
    cin >> a >> b ;
    //*/
    
    /*
    vector<int> a(3);
    cin >> a.at(0) >> a.at(1) >> a.at(2) ;
    
    sort(a.begin(), a.end());
    */
    
    //*
    if( a.at(0) == b.at(2) && a.at(1) == b.at(1) && a.at(2) == b.at(0) ){
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }
    //*/
    
    //cout << b * 2 - a << endl;

    return 0; 
}
