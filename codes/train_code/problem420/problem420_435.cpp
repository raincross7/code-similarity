#include <bits/stdc++.h>

using namespace std;

#define pb push_back
#define eb emplace_back

typedef long long ll;
typedef unsigned long long ull;

// 45106 - 111

// 77 - 16 41


void oneCase() {
    char a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    auto ok = a == f && b == e && c == d;

    cout << (ok ? "YES\n" : "NO\n");




}


int main() {
    int t = 1;
//    cin >> t;
    while (t--) {
       oneCase();
    }

    return 0;
}
