#include <bits/stdc++.h>

using namespace std;
int main()
{
    int n,k,temp;
    cin>>n>>k;
    temp=n;
    while(n--){
            int a[n];
        cin>>a[n];
    }

    cout<<ceil((double)(temp-1)/(k-1));
    }
