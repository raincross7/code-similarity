#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {

	int n, k; cin >> n >> k;
	
	int is_1_Index;
	for (int i = 0; i < n; i++) {
		int tmp_a; cin >> tmp_a;
	}

	cout << ceil((double)(n - 1) / (k - 1)) << endl;
	return 0;
}