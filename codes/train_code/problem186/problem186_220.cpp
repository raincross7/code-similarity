#include <bits/stdc++.h>
using namespace std;
int main(){
        int n, k; cin >> n >> k;
        for (int x= 0; x < n; ++x){
            int q; cin >> q;
        }
        n = ceil((n-1) * 1.00 / (k-1));
        cout << n << endl;
    return 0;
}
