#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int a, b;
   cin >> a >> b ;
 if ( a == (a/2)*2 || b == (b/2)*2 ){
   cout << "Even" << endl;
 }
  else{
    cout << "Odd" << endl;
  }
  }