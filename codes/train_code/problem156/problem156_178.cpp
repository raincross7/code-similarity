#include <iostream>
#include <cmath>
#include <cstdio>

//#define M_PI 3.14159265358979 /* ?????¨??? */
#define rad2deg(a) ( (a) / M_PI * 180.0 ) /* rad ??? deg ??????????????????????????¢??° */
#define deg2rad(a) ( (a) / 180.0 * M_PI ) /* deg ??? rad ??????????????????????????¢??° */

using namespace std;



int main() {
	double a,b,d;
	
	cin >> a >> b >> d;
	
	printf("%lf\n%lf\n%lf\n", (a*b*sin(deg2rad(d)))/2, a + b + sqrt(a*a + b*b - 2*a*b*cos(deg2rad(d))), b*sin(deg2rad(d)));
	
	return 0;
}