#include <iostream>
#include <cmath>
#define rad(x) (x*3.14159265359/180)
 
using namespace std;
 
int main() {
        double a,b,C;
    
    cin >> a;
    cin >> b;
    cin >> C;
    
    cout.precision(4); // ????????? 4????????????????????????
    cout.setf(ios::fixed); // ?属???属??即?????????????????即?????????
    
    cout << 1.0 / 2.0 * a * b * sin(rad(C)) << endl;
    cout << a+b+ sqrt(a*a+b*b-2.0*a*b*cos(rad(C))) << endl;
    cout << b*sin(rad(C)) << endl;
    
        return 0;
}
 