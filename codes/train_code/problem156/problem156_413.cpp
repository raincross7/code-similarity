#include<iostream>
#include<cmath> // sqrt???pow????????????????????????
#include<cstdio>
#define PI 3.1415926535 //????¨?????????°??????
using namespace std;
 

int main() {
	double a,b,C,S,L,h;
	cin >> a >> b >> C;
	
	double radC = C / 180 * PI; // ?????°?????§?????????????????¢???(??§??????)????????????????????????
	S = a * b * sin(radC) / 2;
	L = a + b + sqrt( pow(a,2) + pow(b,2) - 2 * a *b * cos(radC) );
	h =S / a * 2;

	printf("%.5f\n",S);
	printf("%.5f\n",L);
	printf("%.5f\n",h);

	return 0;
}