#include<bits/stdc++.h>
#pragma optimize("Ofast")
using namespace std;

using ll = long long;
const int N = 1e5+5;

int main()
{
    ios_base::sync_with_stdio(0);

    char ch;
    cin >> ch;
    if(ch == 'A') cout << "T\n";
    if(ch == 'T') cout << "A\n";
    if(ch == 'C') cout << "G\n";
    if(ch == 'G') cout << "C\n";
    return 0;
}
