#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); i++)
#define rep2(i, a, b) for(int i = (a); i < (b); i++)
typedef long long ll;

int main() {
   int y, m, d;
   char s1, s2;
   cin >> y >> s1 >> m >> s2 >> d;

   if (m <= 4) {
       cout << "Heisei" << endl;
   } else {
       cout << "TBD" << endl;
   }
}