#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, srt, end) for (long long i = (srt); i < (long long)(end); i++)
#define VL vector<ll>
#define INF 100000000

int main(){
    ll y,m,d;
    char c[2];
    cin>>y>>c[0]>>m>>c[1]>>d;
    cout<<(m<=4 ? "Heisei":"TBD");
}