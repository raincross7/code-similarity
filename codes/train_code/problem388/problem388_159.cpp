#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int current_rate, target_rate; //初期値
    double next_rate;
 
    cin >> current_rate >> target_rate;
    next_rate = target_rate*2 - current_rate;
    cout << next_rate << endl;
 

}