#include <bits/stdc++.h>
#include <set>
#define rep(i,n) for (int i=0; i < (n); ++i)
#define all(a) a.begin(), a.end()
using ll = long long;
using namespace std;

// long long l[100];

int main() {
  int r,g; cin >> r >> g;
  double ans = 2*g - r;
  cout << ans << endl;
  return 0;
}