#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    char a;
    cin >> a;
    if (islower(a)) {
        cout << 'a';
    } else {
        cout << 'A';
    }
}
