#include <bits/stdc++.h>
#define _GLIBCXX_DEBUG
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(v) v.begin(), v.end()

int main(void){

    char a; cin >> a;
    if('a' <= a && a <= 'z') cout << "a\n";
    else cout << "A\n";

    return 0;
}