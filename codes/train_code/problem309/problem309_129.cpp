#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  char t;
  cin >> t;
  int a = (int)t;
  if (a >= 'A' && a <= 'Z')
    cout << 'A' << endl;
  else if (a >= 'a' && a <= 'z')
    cout << 'a' << endl;
}
