#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <utility>
#include <map>
#include <queue>
using namespace std;
typedef long long ll;
#define rep(i,n) for (int i=0;i < (int)(n);i++)

int main(){
  char x;
  cin >> x;
  if (isupper(x)) cout << 'A' << endl;
  else cout << "a"<< endl;
  return 0;
}
