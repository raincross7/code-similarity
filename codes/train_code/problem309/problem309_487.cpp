#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (int)(n); ++i)
#define MOD 1000000007

using namespace std;
using ll = long long;

int main() {
	char s;
	cin >> s;
	if('a'<=s && s<='z') cout << 'a' << endl;
	else cout << 'A' << endl;;
	return 0;
}
