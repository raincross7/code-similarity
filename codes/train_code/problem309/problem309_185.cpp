#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pint = pair<int, int>;

int main() {
	char a;
	cin >> a;

	if ('a' <= a && a <= 'z') {
		cout << "a" << endl;
	}
	else if ('A' <= a && a <= 'Z') {
		cout << "A" << endl;
	}
	return 0;
}
