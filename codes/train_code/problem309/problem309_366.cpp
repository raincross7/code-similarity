#include <bits/stdc++.h>
#include <iostream>
#include <string>
#include <string.h>

using namespace std;

int main() {
  char ch;
  cin >> ch;

  if('a' <= ch && ch <= 'z')
    cout << 'a' << endl;
  else if('A' <= ch && ch <= 'Z')
    cout << 'A' << endl;
}