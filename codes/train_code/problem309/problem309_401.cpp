#include <bits/stdc++.h>
using namespace std;

int main() {
  char c;
  cin >> c;
  if ('a' <= c && c <= 'z') {
    cout << 'a' << endl;
  }
  if ('A' <= c && c <= 'Z') {
    cout << 'A' << endl;
  }
  // char b;
  // b = 'a';
  // b = b + 3;
  // cout << b;
	return 0;
}