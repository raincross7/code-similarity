#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    char a;
    cin>>a;
    if((int)a>=65 && (int)a<=90)
        cout<<"A";
    else
        cout<<"a";
}
