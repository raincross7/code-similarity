#include <bits/stdc++.h>
using namespace std;

int main() {
  char c;
  cin >> c;
  if (c >= 'A' && c <= 'Z') {
    cout << 'A' << endl;
  }
  else if (c >= 'a' && c <= 'z') {
    cout << 'a' << endl;
  }
}