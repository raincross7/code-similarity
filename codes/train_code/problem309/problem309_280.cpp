#include <bits/stdc++.h>
#define rep(i, n) rep2(i, 0, n)
#define rep2(i, m, n) for (int i = m; i < (n); i++)
typedef long long ll;
using namespace std;

int main(){
   char alfa;
  cin >> alfa;
  char ans = 'a';
  if(alfa-'a'<0)ans='A';
  cout << ans << endl;
}