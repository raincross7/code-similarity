#include <bits/stdc++.h>
#include <vector>
using namespace std;
using ll = long long;
#define rep(i,n) for (ll i=0; i < (ll)(n); i++)

int main(void){
    char x;
    cin>>x;
    if(isupper(x))cout<<'A'<<endl;
    else cout<<'a'<<endl;
}