#include <bits/stdc++.h>
#include <algorithm>
using namespace std;    
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    char c;
    char ans;
    cin >> c;
    if('a'<=c && c<='z'){
        cout << 'a' << endl;
    }else{
        cout << 'A' << endl;
    }
}