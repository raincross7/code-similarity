#include <bits/stdc++.h>
using namespace std;
#define rep(i, n)                    \
  for (int i = 0; i < (int)(n); i++) \
    ;

#define _GLIBCXX_DEBUG
#define _LIBCPP_DEBUG 0

int main() {
  char c;
  cin >> c;

  if (c <= 'Z') {
    cout << 'A' << endl;
  } else {
    cout << 'a' << endl;
  }
}
