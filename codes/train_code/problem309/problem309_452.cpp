#include <bits/stdc++.h>
#define REP(i, n) for (int i = 0; i < (n); ++i)
#define REPS(i, a, b) for (int i = (a); i < (b); ++i)
using namespace std;
using ll = long long;

int main() {
  char a;
  cin >> a;
  
  if((int)a <= 122 && 97 <= (int)a){
    cout << 'a' << endl;
  }
  else{
    cout << 'A' << endl;
  }
    
  

}