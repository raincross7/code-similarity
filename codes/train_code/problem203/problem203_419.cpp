#include <bits/stdc++.h>

using namespace std;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int d, t, s; cin >> d >> t >> s;
    cout << (t * s >= d ? "Yes" : "No") << endl;
    return 0;
}

