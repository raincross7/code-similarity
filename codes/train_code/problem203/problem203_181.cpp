#include <bits/stdc++.h>

using namespace std;

int main() {
    cout << fixed << setprecision(0);
    int D, T, S;
    cin >> D >> T >> S;
    cout << (T * S >= D ? "Yes" : "No") << endl;
    return 0;
}