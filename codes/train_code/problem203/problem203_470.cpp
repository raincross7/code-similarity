#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main () {
    ll D, T, S;
    cin >> D >> T >> S;
    if (S*T < D) {
        puts("No");
    }
    else {
        puts("Yes");
    }
}