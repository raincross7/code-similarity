#include <bits/stdc++.h>

using namespace std;

int main(){
    cin.sync_with_stdio(0);
    cin.tie(0);
    int i, n;
    double d, s, t;
    cin >> d >> t >> s;
    cout << (s * t >= d ? "Yes" : "No") << endl;
}