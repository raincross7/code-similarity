#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0; i<(n); i++)
using ll = long long;
#define int long long
using P = pair<int,int>;
signed main(){
    int a,b;cin >> a >> b;
    bool ok = true;
    if(a > 8 || b > 8)ok = false;
    if(ok)puts("Yay!");
    else puts(":(");
    


    return 0;
}