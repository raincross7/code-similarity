#include <iostream>
#include <stdlib.h>
#include <algorithm>
#include <string>
#include <vector>
#define rep(i, n) for (int i = 0; i < (n); i++)
#define ll long long int
using namespace std;

int main(void) {
  int a, b;
  cin >> a >> b;
  if (a<=8 && b <= 8 ) printf("Yay!\n");
  else printf(":(\n");
  
  return 0;
}
