#include <bits/stdc++.h>
using namespace std;
using ll = long long;

signed main()
{
  cin.tie(0);
  ios::sync_with_stdio(false);

  int a, b;
  cin >> a >> b;

  if (a > 8 || b > 8) cout << ":(\n";
  else cout << "Yay!\n";

  return (0);
}
