#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    if (a > 8 || b > 8) {
        puts(":(");
    } else {
        puts("Yay!");
    }
}