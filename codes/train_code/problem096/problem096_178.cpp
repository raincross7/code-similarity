#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a1, b1;
    string a, b, c;
    cin >> a >> b >> a1 >> b1 >> c;
    //*/

    /*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;

    sort(vec.begin(), vec.end());
    //*/
    
    //*
    if( a == c ){
        a1--;
        //cout << "Yes" << endl;
    } else {
        b1--;
        //cout << "No" << endl;
    }
    //*/
    
    cout << a1 << " " << b1 << endl;
    
    return 0; 
}
