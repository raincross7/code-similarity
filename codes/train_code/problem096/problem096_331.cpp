#include <bits/stdc++.h>
using namespace std;

int main() {
	string a, b; cin >> a >> b;
  	int c, d; cin >> c >> d;
  	string e; cin >> e;
  	if (a==e) c--; else d--;
  	cout << c << " " << d << endl;
}