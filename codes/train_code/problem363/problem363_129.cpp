#include "bits/stdc++.h"

using namespace std;

void Main() {
    int N;
    cin >> N;
    cout << N * (N + 1) / 2 << endl;
}

int main() {
    std::cout << std::fixed << std::setprecision(15);
    Main();
    return 0;
}
