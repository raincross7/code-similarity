#include <bits/stdc++.h>
using namespace std;
int main(){
std::ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    long test;
    test=1;
    // cin>>test;
    while(test--)
    {
        long i,j,n,m,q,a=0,t,b,c,temp,k,maxpeak=0,peak,d;
        cin>>n;
        if(n<1200)cout<<"ABC";
        else cout<<"ARC";
        
        
    }
}