#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
using ll = long long;
using P = pair<int,int>;
int main(void) {
    int x; cin >> x;
    string ans;
    if(x < 1200) ans = "ABC";
    else ans = "ARC";
    cout << ans;
}
