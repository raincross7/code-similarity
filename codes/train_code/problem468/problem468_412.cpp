#include<bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
typedef long long ll;

int main(){
    int x,a;
    cin >> x >> a;
    int b;
    if(x < a) b = 0;
    else b = 10;

    cout << b << endl;
    return 0;
}