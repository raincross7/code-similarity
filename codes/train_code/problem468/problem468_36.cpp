#include<bits/stdc++.h>

using namespace std;

int main() {

	// freopen("input.txt", "r", stdin);
	// freopen("output.txt", "w", stdout);

	int a, x;
	cin >> x >> a;
	if (x < a) {
		cout << "0" << '\n';
	}
	else {
		cout << "10" << '\n';
	}

	return 0;
}