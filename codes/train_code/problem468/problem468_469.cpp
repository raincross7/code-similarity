#include <bits/stdc++.h>
using namespace std;
using Graph = vector<vector<int>>;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
const long long MOD=1e9+7;

int main() {
  int x,a;
  cin>>x>>a;
  if(x<a) cout<<0<<endl;
  else cout<<10<<endl;
  return 0;
}