#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<string,int>;
using T = tuple<string,int,int>;

int main(){
  int n,a;
  cin >> n >> a;
  if(n < a) cout << 0 << endl;
  else cout << 10 << endl;
  return 0;
}