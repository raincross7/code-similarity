#include<bits/stdc++.h>
using namespace std;

#define LL long long

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    
    int x, a; cin >> x >> a;
    if(x < a) cout << 0;
    else cout << 10;    
}