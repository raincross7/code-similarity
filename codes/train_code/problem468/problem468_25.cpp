#include <bits/stdc++.h>
using ll = long long;
using namespace std;
using Graph = vector<vector<ll>>;

int main(){
  int x,a;cin>>x>>a;
  if (x < a)
  {
    cout << 0 << endl;
  }
  else if (x >= a)
  {
    cout << 10 << endl;
  }
  
  
}