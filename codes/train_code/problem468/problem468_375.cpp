#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
typedef long long ll;

int main(){
  int x, a;
  cin >> x >> a;
  if(x<a){
    cout << 0 << endl;
  }
  else{
    cout << 10 << endl;
  }

  return 0;
}