#include <bits/stdc++.h>
#define rep(i,n) for(int i=0; i<(n); i++)
using namespace std;
using ll = long long;

int main() 
{
    int x, a;
    cin >> x >> a;

    if (x<a) puts("0");
    else puts("10");
    return 0;
}
