#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int x, a;
    cin >> x >> a;

    if (x < a) cout << 0 << endl;
    else if (x >= a) cout <<10 << endl;

    return 0;
}