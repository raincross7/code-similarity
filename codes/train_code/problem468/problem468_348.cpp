#include <bits/stdc++.h>
using namespace std;
using ll=long long;
#define FOR(i,a,b) for(ll i=a;i<b;i++)
#define rep(i,n) FOR(i,0,n)

int main() {
  ll X, A;cin >> X >> A;
  if (X >= A) cout << 10 << endl;
  else cout << 0 << endl;
}