#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b;
    cin >> a >> b ;
    //*/

    /*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;
    
    sort(vec.begin(), vec.end());
    //*/
    
    //*
    if( a < b ){
        cout << 0 << endl;
    } else {
        cout << 10 << endl;
    }
    //*/
    
    //cout << vec.at(0) + vec.at(1) << endl;

    return 0; 
}
