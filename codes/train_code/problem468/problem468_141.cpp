#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (ll i=0; i<(n); ++i)
typedef long long ll;
bool debug=false;
string yes = "Yes";
string no = "No";

int main(){
  int x,a; cin >> x >> a;
  cout << ((x<a ) ? 0 : 10) << endl;
  return 0;
}

