#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using ii = pair<int, int>;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int x, a;

  cin >> x >> a;

  if (x < a) cout << 0;
  else cout << 10;
  cout << endl;
  
  return 0;
}
