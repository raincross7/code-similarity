#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;


int main()
{
  int x, a;
  cin >> x >> a;
  if (x < a)
    cout << "0\n";
  else
    cout << "10\n";
  return 0;
}