#include <iostream>
#include <vector>
#include<cmath>
#include <set>
#include <map>
#include<cstring>
#include <queue>
#include <stack>
#include <algorithm>
using namespace std;
#define ll long long
#define N 200005
#define fori for(i=0;i<n;i++)
#define fori1 for(i=1;i<=n;i++)
const int mod = 1e9 + 7;
ll a[3*N];//开三倍
char s[N];
ll degree[N];
ll b[3*N];
ll vis[N];
int main()
{

	ll i, j, k;
	ll n, sum,m;
	ll flag = 1, maxn = 0, minn = mod;
	cin >> n >> m;
	cout << (n * n - m) << endl;
}
