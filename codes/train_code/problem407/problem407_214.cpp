#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define ll long long

using namespace std;

int main() {

    ll n,k; cin >> n >> k;
    cout << (ll)(k*pow(k-1,(n-1)));

    return 0;
}









