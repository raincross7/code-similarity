#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i< (n); i++)
using ll = long long;
using P = pair<int, int>;
int main()
{
    char s;
    cin >> s;
    char ans = (char)(s + 1);
    cout << ans << endl; 
return 0;
}