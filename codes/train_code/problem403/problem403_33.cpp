#include <bits/stdc++.h>
using namespace std;
using ll =long long;

int main(){
    ll a,b; cin>>a>>b;
    ll m=min(a,b),M=max(a,b);
    for (ll i = 0; i < M; i++){
        cout<<m;
    }
    cout<<endl;
    
}