#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

int main(){
  char op;
  ll a,b;
  cin >> a >> op >> b;
  if(op == '+') cout << a + b << endl;
  else cout << a - b << endl;
}
