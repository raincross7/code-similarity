#include <bits/stdc++.h>
#define REP(i, n) for (int i = 0; i < (n); ++i)
#define REP(i, a, b) for (int i = (a); i < (b); ++i)
using namespace std;
using ll = long long;



int main() {
  int a,c;
  char b;
  cin >> a >> b >> c;
  if(b == '+'){
    
    cout << a + c << endl;
  }
  else{
    cout << a - c << endl;
  }
  
  
}