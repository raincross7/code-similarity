#include <iostream>
#include <iomanip>

using namespace std;

int main() {

  int a, c;char b;
  cin >> a >> b >> c;
  double ans, ra, rb;
  string dolphinManJotaro;
  cout << (a + c) * (b == '+') + (a - c) * (b == '-');
  return 0;
}
