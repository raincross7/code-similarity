#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0; i<(n); i++)
typedef long long ll;

int main(){
	string s; cin>>s;
	string f=s.substr(0,4);
	if(f=="YAKI") cout<<"Yes"<<endl;
	else cout<<"No"<<endl;
}