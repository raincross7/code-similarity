#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
  
  	long long a,b,c=0;
  	cin >> a >> b;
  	for (int i=0; i<a; i++){
      long long d;
      cin >> d;
      if (d>=b){c++;}}
    cout << c;}