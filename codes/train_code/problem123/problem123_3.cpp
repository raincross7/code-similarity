#include <bits/stdc++.h>

#define fastI ios_base::sync_with_stdio(false); cin.tie(NULL)
#define endl '\n'

using namespace std;

int main() {
    fastI;
    int n, p;
    cin >> n >> p;
    cout << n - p + 1 << endl;
    return 0;
}