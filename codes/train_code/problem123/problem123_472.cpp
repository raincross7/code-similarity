#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using ld = long double;

int main(){
  int n,i; cin >> n >> i;

  int ans = n - i + 1;

  cout << ans << endl;
}
