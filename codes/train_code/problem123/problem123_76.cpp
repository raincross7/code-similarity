#include <iostream>
using namespace std;
const long INF = 1e9;
const long MOD = 1e9 + 7;
#define repi(i, n, init) for (int i = init; i < int(n); i++)

int main()
{
    int n, i;
    cin >> n >> i;
    cout << n - i + 1 << endl;
    return 0;
}