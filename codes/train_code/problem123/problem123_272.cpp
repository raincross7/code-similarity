#include <bits/stdc++.h>
#include <cstring>

#define pi 2*acos(0.00)
#define ll long long
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define nl '\n'
#define null '\0'

using namespace std;

int main()
{
    fastio;

    int n,p,x;
    cin>>n>>p;
    x=n-p+1;
    cout<<x<<nl;

}