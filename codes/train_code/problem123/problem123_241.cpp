#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    int n, i;
    cin >> n >> i;

    int ans = n - i + 1;

    cout << ans << endl;
    return 0;
}
