#include <iostream>
using namespace std;

int main(void)
{
  int w, h, x, y, r;
  bool flg = true;
  cin >> w >> h >> x >> y >> r;

  // ????????¢??????????????§??????
  if (y - r < 0) flg = false;
  // ???????????§??????
  if (y + r > h) flg = false;
  // ???????????§??????
  if (x - r < 0) flg = false;
  // ???????????§??????
  if (x + r > w) flg = false;
  
  // ??????
  cout << (flg ? "Yes" : "No") << endl;
}