#include <bits/stdc++.h>
using namespace std;
int main()
{
    long i,j,t,a,b,n,c,x,y,m,prev,sum;
    t=1;
    // cin>>t;
    while(t--)
    {
       string s;
       cin>>s;
       for(i=0;i<s.length();i+=2)
       cout<<s[i];
    }
}