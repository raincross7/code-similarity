#include <bits/stdc++.h>
using namespace std;

int main() {
  // 入力部(a,b,c)
  string a, b, c;
  cin >> a >> b >>c;

  cout << (char)(a.at(0)-32) << (char)(b.at(0)-32) << (char)(c.at(0)-32) << endl;
}
