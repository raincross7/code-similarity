#include <bits/stdc++.h>
using namespace std;

int main() {
  string a, b, c;
  cin >> a >> b >> c;
  string res;
  res += toupper(a[0]);
  res += toupper(b[0]);
  res += toupper(c[0]);
  cout << res << endl;
}
