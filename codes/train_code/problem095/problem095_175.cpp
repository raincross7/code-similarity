#include<bits/stdc++.h>

using namespace std;
using ll = long long;


int main()
{
    string a, b, c;
    cin >> a >> b >> c;
    cout << (char)(a.front() + ('A' - 'a')) <<  (char)(b.front() + ('A' - 'a')) <<  (char)(c.front() + ('A' - 'a')) << endl;
}