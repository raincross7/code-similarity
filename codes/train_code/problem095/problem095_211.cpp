#include <bits/stdc++.h>
using namespace std;

int main() {
  string a, b, c;
  cin >> a >> b >> c;
  char A = toupper(a.at(0));
  char B = toupper(b.at(0));
  char C = toupper(c.at(0));
  cout << A << B << C << endl;
}