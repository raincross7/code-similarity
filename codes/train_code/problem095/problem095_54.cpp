#include <iostream>
#include <iomanip>

using namespace std;

int main() {

  int a, c;int b;
  //cin >> a >> b;// >> c; //>> b >> c;
  double ans, ra, rb;
  string FatString;
  cin >> FatString;
  cout << char(FatString[0] - 'a' + 'A');
  cin >> FatString;
  cout << char(FatString[0] - 'a' + 'A');
  cin >> FatString;
  cout << char(FatString[0] - 'a' + 'A');
  return 0;
}
