#include <bits/stdc++.h>

#define rep(l, r) for (int i = (l); i < (r); i++)

typedef long long ll;
 
using namespace std;

int main() {
    string s;
    rep(0, 3) {
        cin >> s;
        cout << char(toupper(s[0]));
    }
    cout << endl;

    return 0;
}
