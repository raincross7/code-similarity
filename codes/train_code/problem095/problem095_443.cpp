#include <bits/stdc++.h>
using namespace std;
map<string,int> mp;
int main() {
    long test=1;
    // cin>>test;
    while(test--)
    {
	long t,i,j,n,a,b,c,d,ans,A,B;
	string s,s1,s2,s3;
	cin>>s1>>s2>>s3;
    cout<<char(int(s1[0])-32);
    cout<<char(int(s2[0])-32);
    cout<<char(int(s3[0])-32);
    }
	return 0;
}