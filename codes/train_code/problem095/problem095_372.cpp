#include <bits/stdc++.h>
using namespace std;

int main() {
    string a,b,c;
    cin >> a >> b >> c;
    
    char A = toupper(a[0]);
    char B = toupper(b[0]);
    char C = toupper(c[0]);
    
    cout << A << B << C;
    return 0;
}