#include <bits/stdc++.h>
#include <vector>
#define rep(i, n) for(int i = 0; i < n; ++i)
#define ll long long
using namespace std;

int main() {
  string a, b, c;
  cin >> a >> b >> c;
  char p, q, r;
  p=a[0]-32;
  q=b[0]-32;
  r=c[0]-32;
  cout << p << q << r << endl;
  return 0;
}
