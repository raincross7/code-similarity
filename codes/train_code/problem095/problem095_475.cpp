#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    locale l = locale::classic();
    string a, b, c;
    cin >> a >> b >> c;
    cout << toupper(a[0], l) << toupper(b[0], l) << toupper(c[0], l) << endl;
}