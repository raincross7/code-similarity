#include <bits/stdc++.h>
using namespace std;

int main(void) {
	int input;
	for(int r = 1; ; r++) {
		cin >> input;
		if(input == 0) break;
		cout << "Case " << r << ": " << input << endl;
	}
	return 0;
}