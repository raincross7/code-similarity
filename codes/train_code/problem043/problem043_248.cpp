#include <bits/stdc++.h>
using namespace std;
#define MIN(a, b) ((a) <= (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))
void search(int n,int m,char**field){
	
}
int main(){
	int n,k,ans;
	//string ans="No";
	cin >> n >> k;
	ans = n - k + 1;
	cout << ans << endl;
	//cout << maxt << endl;
}