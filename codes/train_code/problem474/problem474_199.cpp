#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	string s;
	cin >> s;
	cout << s.substr(0, 4) << " " << s.substr(4, 12) << endl;
	return 0;
}
