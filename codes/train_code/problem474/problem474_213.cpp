#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define REP(i, a, b) for(int i = a, i##_len = (b); i < i##_len; ++i)

int main()
{
    string S;
    cin >> S;
    cout << S.substr(0, 4) << " " << S.substr(4,8) << endl;;
    return 0;
}