#include <bits/stdc++.h>
using namespace std;
int main(){
   char n,x;
   cin>>n>>x;
      if(int(n)>int(x))
        cout<<">";
      else if(int(n)<int(x))
        cout<<"<";
        else
        cout<<"=";


 return 0;
}
