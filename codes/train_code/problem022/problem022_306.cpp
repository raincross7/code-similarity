#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define rep(i, n) for (int i = 0; i < n; ++i)

int main() {
    double a, b;
    cin >> a >> b;
    cout << round((a + b) / 2.0) << endl;

    return 0;
}