#include <iostream>
#include <string>
#include <cmath>
#define rep(i,n) for (int i = 0; i<(n); i++)
using namespace std;
using P = pair<int,int>;
#include <vector>
#include <algorithm>
#include <iostream>
#include <string>
#include <cmath>
#define rep(i,n) for (int i = 0; i<(n); i++)
using namespace std;
using P = pair<int,int>;
#include <vector>
#include <algorithm>
#include <stdlib.h>
#include <set>

int main() {
    int a,b;
    cin >> a>>b;
    double ans; /* 計算結果 */
    ans =( double(a) + double(b))/2.0 + 0.999999;

    cout << (int) ans;

    return 0;
}