#include <bits/stdc++.h>
using namespace std;

typedef signed long long ll;
const int MX = 1e3 + 5;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    (c >= a && c <= b) ? cout << "Yes\n" : cout << "No\n";
    return 0;
}