#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;

int main()
{
  ll x, t;
  cin >> x >> t;
  if(x>t)
    cout << x - t;
  else if(x == t)
    cout << 0;
  else
  {
    cout << 0;
  }

}
