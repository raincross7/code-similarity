#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for (int i = 0; i< (n); ++i)
const int INF = 1001001001;

/* 
 * */
int main() {
    int T, X;
    cin >> T >> X;
    double ans = (double)T / X;
    cout << ans << endl;
    return 0;
}
