#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, n) for (int i = 1; i <= (int)(n); i++)

int main(){
  double T, X;
  cin >> T >> X;

  cout << setprecision(9) << fixed << T / X << endl;
}