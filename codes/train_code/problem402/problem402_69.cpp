#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
#define _GLIBCXX_DEBUG
using namespace std;
using ll = long long;
using P = pair<int,int>;

int main() {
	int T, X;
	cin >> T >> X;
	float ans;
	ans = float(T)/float(X) ;
	cout << ans << endl;
	return 0;
}
