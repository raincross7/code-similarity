#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    double t, x;
    cin >> t >> x;

    double ans = t / x;

    printf("%.10f", ans);
    return 0;
}
