#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;

int main() {
    double a,b;cin>>a>>b;
    cout<<setprecision(10)<<a/b<<endl;
	return 0;
}