#include <bits/stdc++.h>

#define rep(l, r) for (int i = (l); i < (r); i++)

typedef long long ll;
 
using namespace std;

int main() {
    int t, x;
    cin >> t >> x;

    cout << (double(t) / double(x)) << endl;

    return 0;
}
