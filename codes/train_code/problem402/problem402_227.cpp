#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for(int i = 0; i < n;i++)

int main()
{
    int T, X;
    cin >> T >> X;
    cout << setprecision(11) << (double)T / (double)X << endl;
}