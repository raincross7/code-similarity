#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int t, x;
	cin >> t >> x;
	cout << fixed << setprecision(10) << (double) t / x << endl;
	return 0;
}
