#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; ++i)
using namespace std;
using ll = long long;

int main() {
    double T, X;
    cin >> T >> X;
    printf("%.10lf", T/X);
    return 0;
}