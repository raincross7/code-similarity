#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, x;
    scanf("%d%d", &t, &x);
    double ans = double(t) / x;
    cout << fixed << setprecision(10) << ans << endl;
    return 0;
}