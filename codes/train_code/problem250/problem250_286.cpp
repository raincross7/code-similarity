#include "bits/stdc++.h"
 
using namespace std;
 
using ll = long long;
 
#define all(x) begin(x), end(x)
#define rot(s, k) rotate(s.begin(), s.begin()+ s.size()-(k%s.size()), s.end()); 
 
int main(){
ios_base::sync_with_stdio(0); cin.tie(0);
ll n; cin >> n;
cout << fixed << (long double)n/3*(long double)n/3*(long double)n/3 << "\n";

 
}