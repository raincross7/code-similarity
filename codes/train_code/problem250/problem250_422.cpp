#include <bits/stdc++.h>
using namespace std;
int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);
  float l;
  cin>>l;
  float ans=(l*l*l)/27;
  cout<<fixed<<setprecision(15)<<ans;
  return 0;

}