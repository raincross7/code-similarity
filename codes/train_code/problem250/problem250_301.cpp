#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;
using ll = long long;

int main()
{
    double l;
    cin >> l;
    cout << setprecision(10) << pow(l / 3.0, 3.0) << endl;
    return 0;
}