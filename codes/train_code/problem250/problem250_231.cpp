#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <iomanip>

using namespace std;
using ll = long long;

int main() {
    double l;
    cin >> l;
    cout << fixed << setprecision(12) << (l/3) * (l/3) * (l/3) << endl;
}