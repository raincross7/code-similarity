#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
  double l;
  cin >> l;
  double h, w, d;
  h = w = d = l / 3;
  cout << fixed << setprecision(12) << l / 27 * l * l << endl;
}
