#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < n; i++)
using namespace std;
using ll = long long;
using ull = unsigned long long;
using P = pair<int, int>;

int main() {
    double l;
    cin >> l;
    cout << fixed << setprecision(6) << l * l * l / 27 << endl;
    return 0;
}
