#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main(){
  long double a;
  cin >> a;
  
  long double b = 0;
  b = a/3 * a/3 * a/3;
  
  cout <<fixed << setprecision(15) << b << endl;
}
