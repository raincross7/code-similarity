#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {

	int n; cin >> n;
	double l = (double)n / 3;
	cout << fixed << setprecision(11) << l * l * l << endl;
    return 0;
}