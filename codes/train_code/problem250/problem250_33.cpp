#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (n); ++i )

using namespace std;
typedef long long ll;

int main(){
    double L;cin >> L;
    double a = L/3;
    double sum = pow(a,3);
    cout << setprecision(10) << sum << endl;
    return 0;
}