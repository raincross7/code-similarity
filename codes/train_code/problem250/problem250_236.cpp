#include <bits/stdc++.h>
using namespace std;

int main() {
  int L;
  cin >> L;
  
  double l = (double) L /  3;
  
  double re = (double) l*l*l;
  
  cout << fixed << setprecision(16);
  cout << re << endl;
}