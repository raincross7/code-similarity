#include <bits/stdc++.h>
using namespace std;
int main(){
        long double l; cin >> l;
        l = (long double)(l / 3);
        l = (long double)l * (long double)l * (long double)l;
        cout << fixed << setprecision(20);
        cout << l << endl;
    return 0;
}
