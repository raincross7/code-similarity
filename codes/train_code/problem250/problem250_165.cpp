#include <bits/stdc++.h>
#define rep(i, n)	for(int i=0;i<(int)(n);i++)
using namespace std;
using ll=long long;

int main(){
    double l;
    cin>>l;
    double v=pow(l/3.0, 3);
    cout<<fixed<<setprecision(12)<<v<<endl;
}