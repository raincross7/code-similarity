#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  int H, W;
  cin >> H >> W;
  vector<string> C(H);
  for (int i = 0; i < H; ++i) {
    cin >> C.at(i);
    cout << C.at(i) << endl;
    cout << C.at(i) << endl;
  }

  return 0;
}
