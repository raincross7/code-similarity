#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(int i = 0; i < n; i++)

int main()
{
  int h, w;
  cin >> h >> w;
  string s[h];
  REP(i, h)
  {
    cin >> s[i];
  }

  REP(i, h)
  {
    cout << s[i] << endl;
    cout << s[i] << endl;
  }
  return 0;
}