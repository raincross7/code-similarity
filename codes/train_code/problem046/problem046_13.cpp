#include <bits/stdc++.h>
#define rep(i, ns, ne) for (int i = ns; i < ne; ++i)
using namespace std;

int main() {
  int H, W;
  cin >> H >> W;
  string s;
  rep(i, 0, H) {
    cin >> s;
    cout << s << endl << s << endl;
  }
  getchar();
}