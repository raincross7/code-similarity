#include "bits/stdc++.h"
using namespace std;

int main() {
	int H, W;
	cin >> H >> W;
	vector<string> S(H);
	for (int n = 0; n < H; ++n) {
		cin >> S[n];
	}
	int ans = 0;
	for (int n = 0; n < H; ++n) {
		cout << S[n] << endl;
		cout << S[n] << endl;
	}
	return 0;
}