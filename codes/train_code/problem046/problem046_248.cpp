#include <bits/stdc++.h>
#define rep(i , n) for (int i = 0; i < (int)(n); i++)
const int INF = 1<<29;
using namespace std;
using ll = long long;
vector <int> F(30);
int main(){
    int H , W;
    cin >> H >> W;
    vector <string> C(H);
    for(int i = 0; i < H; i++){
        cin >> C[i];
        cout << C[i] << endl;
        cout << C[i] << endl;
    }
}