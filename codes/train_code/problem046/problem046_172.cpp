#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int h, w;
    cin >> h >> w;
    for(int i = 0; i < h; i++)
    {
        string s;
        cin >> s;
        cout << s << "\n" << s << "\n";
    }

    return 0;
}
