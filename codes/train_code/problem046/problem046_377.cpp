#include <bits/stdc++.h>

using namespace std;
using ll = long long int;
using P = pair<int, int>;

#define loop(i, n) for(int i = 0;i < n;i++)

int main(void){

    int h, w;
    cin >> h >> w;

    loop(i, h){
        string str;
        cin >> str;
        cout << str << endl;
        cout << str << endl;
    }

    return 0;
}
