#include<bits/stdc++.h>
using namespace std;

int main(){
    int h, w;
    cin >> h >> w;
    vector<string> pic(h);
    for(int i = 0; i < h; i++){
        cin >> pic[i];
    }
    for(int i = 0; i < h; i++){
        cout << pic[i] << endl;
        cout << pic[i] << endl;
    }
}