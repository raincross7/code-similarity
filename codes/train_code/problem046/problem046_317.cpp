#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
using ll = long long;
using P = pair<int, int>;

int h, w;
string cline;

int main() {
  cin >> h >> w;
  rep (i, h) {
    cin >> cline;
    cout << cline << endl;
    cout << cline << endl;
  }
  return 0;
}
