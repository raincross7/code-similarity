#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
using ll=long long;

int main() {
    int n,a;
    cin>>n>>a;
    
    if(a>=n%500)cout<<"Yes";
    else cout<<"No";
    
    return 0;
    }
    
