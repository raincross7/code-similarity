/**************
*	???????????????
*************/
//for?????????while?????????vector?????????list??????


#include <iostream>
#include <cmath>		//	????§???¢??°
using namespace std;

struct Dot{
	double x;
	double y;
};

//??????
void output_xy(Dot a){
	cout << a.x  <<" "<< a.y << endl;
}

	//2?????????????????¨3????????§?¨??????¨???????????¢??°
void koch(Dot start, Dot end, int n )
{
	if(n == 0){
		return;
	}
	
	
	Dot v;//???
	v.x = (end.x-start.x)/3;
	v.y = (end.y-start.y)/3;
	
	Dot s;//?????¨????????????
	s.x = start.x+v.x;
	s.y = start.y+v.y;
	
	Dot t;
	t.x = start.x+2*v.x;
	t.y = start.y+2*v.y;
	
	Dot u;
	u.x = s.x + v.x*cos(M_PI*60/180)-v.y*sin(M_PI*60/180);
	u.y = s.y + v.x*sin(M_PI*60/180)+v.y*cos(M_PI*60/180);
	
	n --;
	koch(start, s, n);
	output_xy(s);		//????????????????????????
	koch(s, u, n);
	output_xy(u);
	koch(u, t, n);
	output_xy(t);
	koch(t, end, n);

}
//-------------------------------------------------------------
int main()
{
	int n;
	cin >> n;
	
	Dot start;
	start.x =0;
	start.y =0;
	
	Dot end;
	end.x = 100;
	end.y =   0;
	
	output_xy(start);
	koch(start, end, n);
	output_xy(end);
	
	return 0;
}