#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
using ll = long long;
using p = pair<int,int>;



int main() {
  int A,B; cin >> A >> B;
  int ans = (A-1) * (B-1);
  cout << ans << endl;




  return 0;
}
