#include <bits/stdc++.h>

#define rep(l, r) for (int i = (l); i < (r); i++)

typedef long long ll;
 
using namespace std;

int main() {
    int n;
    cin >> n;

    cout << ((n * 800) - (int(n / 15) * 200)) << endl;

    return 0;
}
