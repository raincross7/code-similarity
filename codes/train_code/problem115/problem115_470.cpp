#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
	int s, w; cin >> s >> w;
  	cout << (w >= s ? "unsafe" : "safe") << endl;
    return 0;
}
