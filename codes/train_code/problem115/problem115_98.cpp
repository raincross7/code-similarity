#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int s, w;
    cin >> s >> w;
    cout << (w>=s ? "unsafe" : "safe");

    return 0;
}
