#include <iostream>
#include <iostream>

using namespace std;

int main() {

  int a, b, k, poo;
  cin >> a >> b;
  poo = ((a - 1) / k) * k + k;
  if(!(a > b)){
    cout << "un";
  }
  cout << "safe";
  return 0;
}
