#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int,int>;

void solve() {
    int s, w;
    cin >> s >> w;

    cout << (s <= w ? "unsafe" : "safe") << endl;
}

int main() {
    solve();
    return 0;
}
