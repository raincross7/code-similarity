#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;

int main() {
  int S, W;
  cin >> S >> W;
  cout << (W >= S ? "unsafe" : "safe") << endl;
}