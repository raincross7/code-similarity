#include<algorithm>
#include<cmath>
#include<iostream>
#include<vector>
#include<string>
using namespace std;
typedef long long ll;

#define REP(i, n) for(ll i=0;i<ll(n); i++)

int main(){
    int s, w;
    cin >> s >> w;
    if(w>=s){cout << "unsafe" << endl;return 0;}
    else{cout << "safe" << endl; return 0;}

    return 0;
}
