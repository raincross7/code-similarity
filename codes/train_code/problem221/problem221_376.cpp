#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
#define rep(i,n) for (int (i)=0; (i) < (n); (i)++)

int main()
{
    ll N, K; cin >> N >> K;

    if (N % K == 0) cout << 0 << endl;
    else cout << 1 << endl;
}
