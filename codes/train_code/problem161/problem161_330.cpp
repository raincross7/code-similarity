#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); i++)
#define rep2(i, a, b) for(int i = (a); i < (b); i++)
typedef long long ll;

int main() {
   char a, b;
   cin >> a >> b;
   if (a == b) {
       cout << 'H' << endl;
   } else {
       cout << 'D' << endl;
   }
}