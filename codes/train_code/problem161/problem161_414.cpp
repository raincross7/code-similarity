#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define v_inputrep(v,n) for(int i=0;i<n;i++){cin >> v.at(i);} //配列vにn個の要素を入力する


int main(){
    char a,b;
    cin >> a >> b;
    if(a==b){cout<<"H";}
    else{cout<<"D";}
}