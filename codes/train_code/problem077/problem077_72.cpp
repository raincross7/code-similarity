#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
    ll N;
    cin >> N;

    ll ans =(N-1) * N / 2;

    cout << ans << endl;
    return 0;
}