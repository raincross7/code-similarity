#include <bits/stdc++.h>
using namespace std;
#define rep(i,A) for(int i = 0;i < A;i++)

int main() {
long long A,B,C,D;
  cin >> A >> B >> C >> D;
 cout << max(max(A * C,A * D),max(B * C,B * D));

 
}
