#include <bits/stdc++.h>
using namespace std;
using ll = long long;

signed main()
{
  cin.tie(0);
  ios::sync_with_stdio(false);

  int k, x;
  cin >> k >> x;

  if (k * 500 >= x) cout << "Yes\n";
  else cout << "No\n";

  return (0);
}