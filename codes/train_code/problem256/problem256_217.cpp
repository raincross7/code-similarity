#include <bits/stdc++.h>
#define f(i, a, b) for(int i = a; i < (b); ++i)
#define rep(i,n) f(i, 0, n)
using namespace std;

int main(void){
  int k, x;
  cin >> k >> x;
  cout << (500*k>=x?"Yes":"No") << endl;
  return 0;
}