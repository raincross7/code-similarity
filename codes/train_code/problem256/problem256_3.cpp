#include "bits/stdc++.h"

using namespace std;

void Main() {
    int K, X;
    cin >> K >> X;
    cout << (500 * K >= X ? "Yes" : "No") << endl;
}

int main() {
    std::cout << std::fixed << std::setprecision(15);
    Main();
    return 0;
}
