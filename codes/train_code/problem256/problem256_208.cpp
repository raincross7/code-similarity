#include <bits/stdc++.h>
using namespace std;

#define ll long long 

int main() {
    ios::sync_with_stdio(0); 

    int k, x;
    cin >> k >> x;

    cout << (k * 500 >= x ? "Yes" : "No") << "\n"; 

    return 0; 
}

