#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const char nl = '\n';

// INT VS LL

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,x;
    cin>>n>>x;
    if (n*500>=x){
    	cout<<"Yes"<<nl;
    }
    else cout<<"No"<<nl;

    return 0;
}