#include<bits/stdc++.h>
using namespace std; 
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define length size()
#define endl "\n"
signed main(void){
 float a,b,c;
  cin >> a >> b >> c;
  cout << (int)(c*(ceil(a/b)))+0 << endl;
}
