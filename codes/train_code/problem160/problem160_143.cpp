#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const char nl = '\n';

// INT VS LL

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s,t;
    cin>>s>>t;
    cout<<(string)t+s<<nl;

    return 0;
}